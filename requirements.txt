# TokenDNA — Python dependencies
# Pin major versions for reproducibility; use `pip install -r requirements.txt`

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.29.0
python-multipart==0.0.22

# ── JWT / OIDC ────────────────────────────────────────────────────────────────
python-jose[cryptography]==3.4.0
cryptography==46.0.6

# ── HTTP client (JWKS fetch, webhooks, AbuseIPDB, ip-api) ────────────────────
requests==2.33.0
httpx==0.28.1                    # async-capable alternative used by some deps
psycopg[binary]==3.3.3           # PostgreSQL backend for production data plane

# ── Redis ─────────────────────────────────────────────────────────────────────
redis==7.3.0

# ── ClickHouse ────────────────────────────────────────────────────────────────
clickhouse-connect==0.15.1

# ── AWS connector (optional — only needed when using AWS onboarding) ──────────
boto3==1.34.100
botocore==1.34.100

# ── GeoIP (optional — only needed when GEOIP_PROVIDER=maxmind) ───────────────
# geoip2==4.8.0                  # uncomment + download GeoLite2-City.mmdb

# ── User-agent parsing ────────────────────────────────────────────────────────
user-agents==2.2.0

# ── ML / adaptive scoring ────────────────────────────────────────────────────
numpy==1.26.4                    # lightweight — used for profile distance math

# ── Utilities ─────────────────────────────────────────────────────────────────
pydantic==2.13.3                  # FastAPI model validation (v2 API)
python-dotenv==1.2.1             # load .env in development
eval_type_backport>=0.2.0   # Python 3.9 compat for PEP 604 type unions
