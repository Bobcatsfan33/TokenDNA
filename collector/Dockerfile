# TokenDNA Collector — production container image (placeholder).
#
# Sprint 1-2 follow-up commit will replace this stub with a real
# multi-stage / distroless image targeting < 50 MB compressed,
# matching the security posture of the platform Dockerfile at the
# repository root.
#
# Until then, this file is a placeholder so the directory tree is
# complete and the release-docker workflow can be templated against
# this path.

FROM python:3.12-slim AS placeholder

LABEL org.opencontainers.image.title="TokenDNA Collector (placeholder)" \
      org.opencontainers.image.licenses="Apache-2.0" \
      org.opencontainers.image.source="https://github.com/Bobcatsfan33/TokenDNA"

CMD ["python", "-c", "raise SystemExit('TokenDNA Collector image not yet built; see Sprint 1-2 follow-up.')"]
